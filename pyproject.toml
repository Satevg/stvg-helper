[project]
name = "stvg-helper"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "python-telegram-bot==21.10",
]

[dependency-groups]
dev = [
    "boto3",
    "black",
]

[tool.black]
line-length = 120
target-version = ["py312"]
